Router and dock registration helpers.

# Responsibilities

Register dock panels, expose route-like APIs for opening/closing panels, and keep dock registry state.

# Usage

- Panels should register themselves with `dockRegistry`.
- Use `index.ts` to create new routes.
