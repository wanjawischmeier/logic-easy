Helpers and factories used to create new project types.

# Responsibilities

Files in here should define a default panel layout and default save state for a given project type.

# Usage

Invoked by the project creation popup or CLI/import workflows when a user creates/opens a project.
