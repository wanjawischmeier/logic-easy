Contains small, focused UI primitives used by components in `src/components`.

# Responsibilities

Small, reusable widget-like components (switches, list styles) that have minimal external dependencies.

# Usage

Import into higher-level components; prefer prop-driven APIs and emit events for state changes.
